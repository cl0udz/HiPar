"use strict";

require("core-js/modules/es.object.define-property");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _buffer = require("buffer");

var _create_buffer = _interopRequireDefault(require("./create_buffer"));

var _define_crc = _interopRequireDefault(require("./define_crc"));

function _interopRequireDefault(obj) {
  return obj && obj.__esModule ? obj : {
    "default": obj
  };
}

var crc1 = (0, _define_crc["default"])('crc1', function (buf, previous) {
  if (!_buffer.Buffer.isBuffer(buf)) buf = (0, _create_buffer["default"])(buf);
  var crc = ~~previous;
  var accum = 0;

  for (var index = 0; index < buf.length; index++) {
    var _byte = buf[index];
    accum += _byte;
  }

  crc += accum % 256;
  return crc % 256;
});
var _default = crc1;
exports["default"] = _default;