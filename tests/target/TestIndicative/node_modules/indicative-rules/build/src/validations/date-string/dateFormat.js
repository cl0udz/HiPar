"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var indicative_utils_1 = require("indicative-utils");

var dateFormat_1 = require("../../raw/dateFormat");

var MISSING_VALUE = 'dateFormat:make sure to define atleast one date format';
var validation = {
  async: false,
  compile: function compile(args) {
    indicative_utils_1.ensureLength(args, MISSING_VALUE, 1);
    return args.map(function (arg) {
      return String(arg);
    });
  },
  validate: function validate(data, field, args, config) {
    var fieldValue = indicative_utils_1.getValue(data, field);
    return indicative_utils_1.skippable(fieldValue, field, config) || dateFormat_1.dateFormat(fieldValue, args);
  }
};
exports["default"] = validation;