"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var indicative_utils_1 = require("indicative-utils");

var validation = {
  async: false,
  validate: function validate(data, field, _args, config) {
    var fieldValue = indicative_utils_1.getValue(data, field);

    if (indicative_utils_1.skippable(fieldValue, field, config)) {
      return true;
    }

    var castedValue = indicative_utils_1.cast(fieldValue, 'integer');

    if (castedValue === null) {
      return false;
    }

    if (castedValue !== indicative_utils_1.cast(fieldValue, 'float')) {
      return false;
    }

    indicative_utils_1.patchValue(data, field, castedValue);
    return true;
  }
};
exports["default"] = validation;