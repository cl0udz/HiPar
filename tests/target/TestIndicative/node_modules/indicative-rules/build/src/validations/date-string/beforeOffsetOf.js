"use strict";

function _slicedToArray(arr, i) {
  return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest();
}

function _nonIterableRest() {
  throw new TypeError("Invalid attempt to destructure non-iterable instance");
}

function _iterableToArrayLimit(arr, i) {
  if (!(Symbol.iterator in Object(arr) || Object.prototype.toString.call(arr) === "[object Arguments]")) {
    return;
  }

  var _arr = [];
  var _n = true;
  var _d = false;
  var _e = undefined;

  try {
    for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {
      _arr.push(_s.value);

      if (i && _arr.length === i) break;
    }
  } catch (err) {
    _d = true;
    _e = err;
  } finally {
    try {
      if (!_n && _i["return"] != null) _i["return"]();
    } finally {
      if (_d) throw _e;
    }
  }

  return _arr;
}

function _arrayWithHoles(arr) {
  if (Array.isArray(arr)) return arr;
}

Object.defineProperty(exports, "__esModule", {
  value: true
});

var indicative_utils_1 = require("indicative-utils");

var utils_1 = require("../../utils");

var beforeOffsetOf_1 = require("../../raw/beforeOffsetOf");

var MISSING_VALUES = 'beforeOffsetOf:make sure to define offset unit and key';
var INVALID_AFTER_KEY = 'beforeOffsetOf:2nd argument must be a valid calc key';
var INVALID_AFTER_UNIT = 'beforeOffsetOf:1st argument must be a number';
var validation = {
  async: false,
  compile: function compile(args) {
    indicative_utils_1.ensureLength(args, MISSING_VALUES, 2);
    var diffUnit = indicative_utils_1.cast(args[0], 'integer', INVALID_AFTER_UNIT);
    var key = args[1];

    if (!utils_1.dateOffsetKeys.includes(key)) {
      throw new Error(INVALID_AFTER_KEY);
    }

    return [Number(diffUnit), key];
  },
  validate: function validate(data, field, _ref, config) {
    var _ref2 = _slicedToArray(_ref, 2),
        diffUnit = _ref2[0],
        key = _ref2[1];

    var fieldValue = indicative_utils_1.getValue(data, field);
    return indicative_utils_1.skippable(fieldValue, field, config) || beforeOffsetOf_1.beforeOffsetOf(fieldValue, diffUnit, key);
  }
};
exports["default"] = validation;