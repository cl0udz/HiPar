"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var indicative_utils_1 = require("indicative-utils");

var linksRegex = /<a\b[^>]*>(.*?)<\/a>/g;
var stripLinks = {
  sanitize: function sanitize(data, field) {
    var fieldValue = indicative_utils_1.getValue(data, field);

    if (typeof fieldValue !== 'string') {
      return;
    }

    indicative_utils_1.patchValue(data, field, fieldValue.replace(linksRegex, function (_match, group) {
      return group.trim();
    }));
  }
};
exports["default"] = stripLinks;