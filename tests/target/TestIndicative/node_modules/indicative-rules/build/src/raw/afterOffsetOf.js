"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var after_1 = require("./after");

var utils_1 = require("../utils");

exports.afterOffsetOf = function (input, diffUnit, key) {
  var expectedDate = utils_1.calcUnits(diffUnit, key, '+');
  return expectedDate ? after_1.after(input, expectedDate) : false;
};