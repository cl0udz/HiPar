"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.sorted = function (input) {
  if (!Array.isArray(input)) {
    return false;
  }

  var scaledDown = false;
  var i = 0;

  while (i < input.length) {
    var b = Number(input[i++]);
    var a = Number(input[i - 2]);

    if (a && a > b) {
      scaledDown = true;
      break;
    }
  }

  return !scaledDown;
};