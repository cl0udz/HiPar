"use strict";

var _require = require('rollup'),
    rollup = _require.rollup;

var _require2 = require('rollup-plugin-uglify'),
    uglify = _require2.uglify;

rollup({
  input: './src/validator.js',
  plugins: [uglify()],
  output: {
    file: './dist/validator.js',
    format: 'umd'
  }
});