"use strict";

module.exports = function (t) {
  var deletes = ['y', 'z', 'lol'];
  t.plan(4);
  return {
    onExportDelete: function onExportDelete(file, name) {
      t.equal(deletes.shift(), name);
    }
  };
};