"use strict";

module.exports = function (t) {
  t.plan(3);
  return {
    // Should bail out twice.
    onModuleBailout: function onModuleBailout(module, bailouts) {
      t.pass('bailed out');
    }
  };
};