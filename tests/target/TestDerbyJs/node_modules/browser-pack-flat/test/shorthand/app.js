"use strict";

var globalVar = 0;

var importedModule = require('./importedModule');

function setExports(argument) {
  var localVar = 1;
  module.exports = {
    globalVar: globalVar,
    importedModule: importedModule,
    localVar: localVar,
    argument: argument
  };
}

setExports(10);