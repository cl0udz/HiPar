'use strict';

var test = require('tap').test;

var _require = require('./util'),
    counTests = _require.counTests,
    runTests = _require.runTests;

var requiredTestSuite = require('./draft6/required.json');

test('required', function (t) {
  var skippedTests = ['ignores arrays', 'ignores strings', 'ignores other non-objects'];
  t.plan(counTests(requiredTestSuite, skippedTests));
  runTests(t, requiredTestSuite, skippedTests);
});