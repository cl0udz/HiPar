'use strict';
/* eslint no-extend-native: off */

var t = require('tap'); // Something could extend the Array prototype


Array.prototype.test = null;
t.doesNotThrow(function () {
  require('../');
});