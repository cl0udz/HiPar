'use strict';

var setLevelSym = Symbol('pino.setLevel');
var getLevelSym = Symbol('pino.getLevel');
var levelValSym = Symbol('pino.levelVal');
var useLevelLabelsSym = Symbol('pino.useLevelLabels');
var changeLevelNameSym = Symbol('pino.changeLevelName');
var useOnlyCustomLevelsSym = Symbol('pino.useOnlyCustomLevels');
var lsCacheSym = Symbol('pino.lsCache');
var chindingsSym = Symbol('pino.chindings');
var parsedChindingsSym = Symbol('pino.parsedChindings');
var asJsonSym = Symbol('pino.asJson');
var writeSym = Symbol('pino.write');
var redactFmtSym = Symbol('pino.redactFmt');
var timeSym = Symbol('pino.time');
var timeSliceIndexSym = Symbol('pino.timeSliceIndex');
var streamSym = Symbol('pino.stream');
var stringifySym = Symbol('pino.stringify');
var stringifiersSym = Symbol('pino.stringifiers');
var endSym = Symbol('pino.end');
var formatOptsSym = Symbol('pino.formatOpts');
var messageKeySym = Symbol('pino.messageKey');
var wildcardFirstSym = Symbol('pino.wildcardFirst'); // public symbols, no need to use the same pino
// version for these

var serializersSym = Symbol.for('pino.serializers');
var wildcardGsym = Symbol.for('pino.*');
var needsMetadataGsym = Symbol.for('pino.metadata');
module.exports = {
  setLevelSym,
  getLevelSym,
  levelValSym,
  useLevelLabelsSym,
  lsCacheSym,
  chindingsSym,
  parsedChindingsSym,
  asJsonSym,
  writeSym,
  serializersSym,
  redactFmtSym,
  timeSym,
  timeSliceIndexSym,
  streamSym,
  stringifySym,
  stringifiersSym,
  endSym,
  formatOptsSym,
  messageKeySym,
  wildcardFirstSym,
  changeLevelNameSym,
  wildcardGsym,
  needsMetadataGsym,
  useOnlyCustomLevelsSym
};