'use strict';

require("core-js/modules/es.date.now");

require("core-js/modules/es.date.to-string");

var nullTime = function nullTime() {
  return '';
};

var epochTime = function epochTime() {
  return ",\"time\":".concat(Date.now());
};

var unixTime = function unixTime() {
  return ",\"time\":".concat(Math.round(Date.now() / 1000.0));
};

module.exports = {
  nullTime: nullTime,
  epochTime: epochTime,
  unixTime: unixTime
};