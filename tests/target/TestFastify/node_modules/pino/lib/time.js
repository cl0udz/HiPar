'use strict';

var nullTime = function nullTime() {
  return '';
};

var epochTime = function epochTime() {
  return `,"time":${Date.now()}`;
};

var unixTime = function unixTime() {
  return `,"time":${Math.round(Date.now() / 1000.0)}`;
};

module.exports = {
  nullTime,
  epochTime,
  unixTime
};