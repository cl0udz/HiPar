"use strict";

require("core-js/modules/es.array.concat");

require("core-js/modules/es.array.concat");

window.loadFile = function (id) {
  var st = ME_SETTINGS;
  location.href = "".concat(st.baseHref, "db/").concat(st.dbName, "/gridFS/").concat(st.bucketName, "/").concat(encodeURIComponent(id));
};